server:
  port: 11100
logging:
  config: config/logback-spring.xml
selenium-hub-url: http://user:1q0p2w9o@192.168.5.75:4442/wd/hub
robots:
  search-system-direct:
    platform: ANY
    browser: chrome
    limit: 25
  google:
    platform: ANY
    browser: chrome
    limit: 15
  google-api:
    search-system-id: 008760942635674233646:iomr_wlnhoi
    key: AIzaSyAw5OFtLD_Jz2m2eRpbzVdEIjWRBCuBNHA
    region:
    search-limit: 15
  yandex:
    platform: ANY
    browser: chrome
    limit: 15
  kaspersky:
    platform: ANY
    browser: chrome
  express:
    platform: ANY
    browser: chrome
  hola:
    platform: ANY
    browser: chrome
    crx-file-path: /home/share/gkojfkhlekighikafcpjkiklfbnlmeio/1.145.963_0
  torguard:
    platform: ANY
    browser: chrome
  hidemyass:
    platform: ANY
    browser: chrome
  cameleoxyz:
    platform: ANY
    browser: chrome
nmap:
  path: nmap
  use-proxy: true
  ports-to-check: 80, 443
spring:
  cloud:
    stream:
      bindings:
        jobs:
          content-type: application/json
          group: executors
        executionResults:
          content-type: application/json
        notifications:
          content-type: application/json
      kafka:
        binder:
          brokers: 192.168.5.12:9094,192.168.5.184:9094,192.168.5.211:9094
        bindings:
          jobs:
            consumer:
              autoCommitOffset: false
              ackEachRecord: true
              configuration:
                max.poll.records: 1
                max.poll.interval.ms: 3600000

---

spring:
  profiles: prod
  cloud.stream:
    bindings:
      jobs:
        destination: jobs
        consumer:
          concurrency: 4
      executionResults:
        destination: exec_results
      notifications:
        destination: job_notifications
    kafka:
      binder:
        brokers: 10.97.220.141:9094,10.97.220.142:9094,10.97.220.143:9094
selenium-hub-url: http://10.97.220.147:4444/wd/hub

---

spring:
  profiles: test
  cloud.stream:
    bindings:
      jobs:
        destination: jobs
        consumer:
          concurrency: 4
      executionResults:
        destination: exec_results
      notifications:
        destination: job_notifications

---

spring:
  profiles: dev_ashabalin
  cloud.stream:
    bindings:
      jobs:
        destination: jobs_ashabalin
        consumer:
          concurrency: 7
      executionResults:
        destination: exec_results_ashabalin
      notifications:
        destination: job_notifications_ashabalin
    kafka.bindings.jobs.consumer:
      configuration:
        max.poll.interval.ms: 300000
nmap:
  path: C:\Program Files (x86)\Nmap
  use-proxy: false

---

spring:
  profiles: dev_asinyavin
  cloud.stream:
    bindings:
      jobs:
        destination: jobs_asinyavin
        consumer:
          concurrency: 7
      executionResults:
        destination: exec_results_asinyavin
      notifications:
        destination: job_notifications_asinjavin
    kafka.bindings.jobs.consumer:
      configuration:
        max.poll.interval.ms: 300000

---

spring:
  profiles: dev_asavin
  cloud.stream:
    bindings:
      jobs:
        destination: jobs_asavin
        consumer:
          concurrency: 7
      executionResults:
        destination: exec_results_asavin
      notifications:
        destination: job_notifications_asavin
    kafka.bindings.jobs.consumer:
      configuration:
        max.poll.interval.ms: 300000

---

spring:
  profiles: dev_pvorobiev
  cloud.stream:
    bindings:
      jobs:
        destination: jobs_pvorobiev
        consumer:
          concurrency: 7
      executionResults:
        destination: exec_results_pvorobiev
      notifications:
        destination: job_notifications_pvorobiev
    kafka.bindings.jobs.consumer:
      configuration:
        max.poll.interval.ms: 300000

---

spring:
  profiles: dev_jspirina
  cloud.stream:
    bindings:
      jobs:
        destination: jobs_jspirina
        consumer:
          concurrency: 7
      executionResults:
        destination: exec_results_jspirina
      notifications:
        destination: job_notifications_srodionova
    kafka.bindings.jobs.consumer:
      configuration:
        max.poll.interval.ms: 300000