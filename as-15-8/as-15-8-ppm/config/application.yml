spring:
  main.allow-bean-definition-overriding: true
  cloud:
    stream:
      bindings:
        outputJobs:
          destination: jobs
        springCloudBusInput:
          destination: bus_config_input
          group: ppm
        springCloudBusOutput:
          destination: bus_config_output
      kafka:
        binder:
          brokers: 192.168.5.12:9094,192.168.5.33:9094,192.168.5.184:9094
          minPartitionCount: 24
          configuration:
            max.request.size: 10485760
        bindings:
          outputJobs:
            producer.configuration:
              key.serializer: org.springframework.kafka.support.serializer.JsonSerializer
  liquibase:
    enabled: true
    change-log: classpath:db/db.changelog.master.xml
  datasource:
    test-while-idle: true
    validation-query: SELECT 1;
    time-between-eviction-runs-millis: 300000
    username: as_user
    password: as_user
  jpa:
    properties:
      hibernate:
        ddl-auto: none
        jdbc:
          lob:
            non_contextual_creation: true
        format_sql: true
    databasePlatform: org.hibernate.dialect.PostgreSQL9Dialect
app:
 #  second, minute, hour, day of month, month, day(s) of week
 schedule: "0 * * ? * *"

---

spring:
  profiles: prod
  profiles.include: docker_log
  main:
    banner-mode: "off"
  datasource:
    url: jdbc:postgresql://10.97.220.144:5432/asdb
  cloud:
    stream:
      kafka.binder:
        brokers: 10.97.220.178:9092,10.97.220.179:9092,10.97.220.180:9092
        minPartitionCount: 250
eureka:
  instance:
    metadata-map:
      expectedAmount: 1

---

spring:
  profiles: test
  profiles.include: docker_log
  main:
    banner-mode: "off"
  datasource:
    url: jdbc:postgresql://192.168.5.211:5432/asdb
eureka:
  instance:
    metadata-map:
      expectedAmount: 1

---

spring:
  profiles: dev_asavin, dev_ashabalin, dev_pvorobiev, dev_asinjavin, dev_danikanov
  profiles.include: local_log
  datasource:
    url: jdbc:postgresql://192.168.5.195:5432/asdb_test
  liquibase:
    enabled: false

---

logging:
  config:
  level:
    root: debug
app:
  #  second, minute, hour, day of month, month, day(s) of week
  schedule: "0 1 1 1 1 *"
spring:
  profiles: dev_asin
  datasource:
    url: jdbc:postgresql://192.168.5.195:5432/asdb_test
  liquibase:
    enabled: false
  cloud:
    stream:
      bindings:
        outputJobs.destination: scheduled_jobs_asin
        springCloudBusInput.destination: bus_config_input_asin
        springCloudBusOutput.destination: bus_config_output_asin

---

spring:
  profiles: dev_asavin
  cloud:
    stream:
      bindings:
        outputJobs.destination: scheduled_jobs_asavin
        springCloudBusInput.destination: bus_config_input_asavin
        springCloudBusOutput.destination: bus_config_output_asavin
  datasource:
    url: jdbc:postgresql://192.168.5.211:5432/asdb_test2
  liquibase:
    enabled: true

---

spring:
  profiles: dev_danikanov
  cloud:
    stream:
      bindings:
        outputJobs.destination: scheduled_jobs_danikanov
        springCloudBusInput.destination: bus_config_input_danikanov
        springCloudBusOutput.destination: bus_config_output_danikanov
  datasource:
    url: jdbc:postgresql://192.168.5.195:5432/asdb
  liquibase:
    enabled: true

---

spring:
  profiles: dev_ashabalin
  cloud:
    stream:
      bindings:
        outputJobs.destination: scheduled_jobs_ashabalin
        springCloudBusInput.destination: bus_config_input_ashabalin
        springCloudBusOutput.destination: bus_config_output_ashabalin
  datasource:
    url: jdbc:postgresql://192.168.5.211:5432/asdb_test
    #url: jdbc:postgresql://192.168.5.195:5432/asdb
    liquibase:
      enabled: true

---

spring:
  profiles: dev_pvorobiev
  cloud:
    stream:
      bindings:
        outputJobs.destination: scheduled_jobs_pvorobiev
        springCloudBusInput.destination: bus_config_input_pvorobiev
        springCloudBusOutput.destination: bus_config_output_pvorobiev

---

spring:
  profiles: dev_asinjavin
  cloud:
    stream:
      bindings:
        outputJobs.destination: scheduled_jobs_asinjavin
        springCloudBusInput.destination: bus_config_input_asinjavin
        springCloudBusOutput.destination: bus_config_output_asinjavin