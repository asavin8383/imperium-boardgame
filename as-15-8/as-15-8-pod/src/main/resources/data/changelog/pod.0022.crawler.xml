<?xml version="1.1" encoding="UTF-8" standalone="no"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.6.xsd">
    <changeSet author="asavin" id="pod.0022">
        <sql>
            alter table sor.domain_masks add created_at timestamptz not null default now();
            alter table sor.domains add created_at timestamptz not null default now();
            alter table sor.domain_masks add updated_at timestamptz not null default now();
            alter table sor.domains add updated_at timestamptz not null default now();
        </sql>
        <rollback/>
    </changeSet>
</databaseChangeLog>